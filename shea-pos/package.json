{
  "name": "shea-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first Shea point of sale",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"tsc -p tsconfig.electron.json --watch\" \"wait-on tcp:5173 file:dist-electron/main.js && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "test:integration": "npm run build && electron dist-electron/integration-test.js",
    "build": "vite build && tsc -p tsconfig.electron.json",
    "dist": "yarn build && electron-builder",
    "dist:win": "yarn build && electron-builder --win nsis"
  },
  "dependencies": {
    "better-sqlite3": "13.0.3",
    "lucide-react": "1.39.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "^4.1.5",
    "zustand": "5.0.15"
  },
  "devDependencies": {
    "@types/better-sqlite3": "9.6.0",
    "@types/node": "^24.10.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "latest",
    "concurrently": "latest",
    "cross-env": "latest",
    "electron": "44.1.1",
    "electron-builder": "26.15.3",
    "typescript": "^5.9.3",
    "vite": "8.2.2",
    "wait-on": "latest"
  },
  "build": {
    "appId": "com.openzey.shea.pos",
    "productName": "Shea POS",
    "asar": true,
    "asarUnpack": ["node_modules/better-sqlite3/**"],
    "files": ["dist/**", "dist-electron/**", "package.json"],
    "directories": { "output": "release" },
    "npmRebuild": true,
    "win": { "target": ["nsis"], "artifactName": "Shea-POS-${version}-${arch}.${ext}" },
    "nsis": { "oneClick": false, "allowToChangeInstallationDirectory": true, "createDesktopShortcut": true }
  }
}

# Shea Local Gateway

Installable Windows service that coordinates multi-POS transactions on a store LAN. See `docs/MULTI-POS.md` for deployment.

Development requires `SHEA_GATEWAY_ENV` to point to a file containing only `DATABASE_URL`, and `SHEA_GATEWAY_CONFIG` to point to the runtime JSON configuration.

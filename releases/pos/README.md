Place signed Shea POS Windows releases in this directory, or publish them from the Shea Admin POS Updates page.

The directory is served at `/downloads/pos` and must contain the generated `latest.yml` beside the installer.

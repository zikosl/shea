import { SectionLayout } from '@/components/admin-panel/section-layout';

export default function CatalogInboxLayout({ children }: { children: React.ReactNode }) {
  return <SectionLayout title="Catalog Inbox">{children}</SectionLayout>;
}
